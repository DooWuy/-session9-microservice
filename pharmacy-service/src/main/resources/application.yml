management:
    endpoints:
        web:
            exposure:
                include: refresh,health,info
server:
    port: 8888
spring:
    cloud:
        config:
            server:
                git:
                    default-label: main
                    uri: https://github.com/your-username/pharmacy-config-repo
